# 作業履歴

作業内容、実施結果、次回開始位置を記録する。新しいエントリは先頭に追加する（新しい順）。

## 記録フォーマット

```
## YYYY-MM-DD タスクID/概要

### 実施内容
- 何を行ったか

### 結果
- 動作確認結果、テスト結果など

### 次回開始位置
- 次に着手すべき場所（ファイル/関数/タスクID）
```

---

## 2026-09-02 T-001: サイト一式の初期登録

### 実施内容
- ユーザーから、note投稿予定のモキュメンタリー・ホラー旅行記の関連素材として作成した架空観光サイト（`sakobuchi_site.zip`）と、その引き継ぎ資料（CLAUDE_CODE_HANDOFF.md）を受け取った。
- リポジトリはproject001テンプレート状態（初期化未実施）だったため、README.mdの「新規プロジェクトでの初期化」手順に従い、docs/tasks.md（T-xxx行・バックログ項目を削除、ヘッダ・区切り行のみ残す）・docs/progress.md（記録フォーマット直後の`---`以下を削除）・docs/decisions.md（同様に`---`以下のD-xxxを削除）・README.mdをこのプロジェクト用にリセットした。
- zipを展開し、`index.html`・`kiroku.html`・`404.html`・`images/`・`.gitignore`をリポジトリ直下へ配置した。
- kiroku.html（隠しページ・ネタバレ）の公開範囲について、リポジトリが既にpublicとして作成済みであることを踏まえユーザーに確認し、「公開のまま追加（何もしない）」の回答を得た。

### 結果
- `index.html`内のリンク（`#about`等のアンカー、`404.html`、`kiroku.html`）を確認し、いずれも実体があるか意図された遷移先であることを確認した。
- `images/`にはREADME.txt（画像追加方法の説明）のみが存在し、写真は未投入（プレースホルダーのみ）であることを確認した。ハンドコフ資料の記載と一致する。

### 次回開始位置
- 特になし。`images/`フォルダへの実際の写真ファイル（AI生成画像）の追加、GitHub Pagesでの公開設定は今後発生しうる別タスク（ユーザー確認が必要な場合あり）。

---

## 2026-09-02 T-002: robots.txt追加とGitHub Pages公開準備

### 実施内容
- ユーザーから「サイトのリンクを出せますか」という依頼を受け、現時点ではGitHub Pages等の公開設定を行っていないためURLが存在しないことを回答した。
- GitHub Pagesで公開する場合の対応方針をユーザーに確認し、「robots.txtで検索エンジンのクロールを拒否しつつ公開する」を選択された（D-002参照）。
- リポジトリ直下に`robots.txt`（`Disallow: /`で全体のクロールを拒否）を追加した。
- GitHub Pagesの有効化自体（リポジトリ設定のSettings > Pages）は、この環境で利用可能なツール（GitHub MCP Server、`gh` CLI）のいずれからも操作できないAPI領域のため実施できなかった。

### 結果
- GitHub MCP Serverのツール一覧・ToolSearchでPages関連の設定変更ツールが存在しないことを確認した。`gh` CLIは本セッションのCapability検出でも`unavailable`（未導入）。

### 次回開始位置
- ユーザーがGitHubのリポジトリ設定でPagesを有効化（Source: mainブランチ、ルート）した後、実際に公開URL（`https://nagamaki0311.github.io/sakobuchi-kankoukyoukai/`想定）でindex.html・kiroku.html・404.htmlの表示を確認する。

---

## 2026-09-02 T-003: kiroku.htmlの恐怖演出強化

### 実施内容
- ユーザーから「kiroku.html（隠しページ）の恐怖演出をリアリティラインを壊さないレベルで強化したい」という依頼を受け、既存の演出（文字コード変換エラーを装った文字化け、本文全体を1文字ずつ揺らすCSS、候文調の由来記）を踏まえた案を複数提示した。静的な案（揺らぎのメリハリ／侵食の段階化／サイドバーのエラー情報追加／結びの改ざん演出／ソースコード内コメント）とアニメーション案（一部の文字の常時微振動／読み込み時の一度きりの明滅／背景ノイズのオーバーレイ／間欠的な一瞬の乱れ）をそれぞれ表で整理し、常時動き続ける演出（背景ノイズ）は「静止した記録を偶然開いてしまった」という既存の空気と相性が悪いとして非推奨とした。
- ユーザーが「静的案のA（メリハリ）・C（エラー情報追加）・D（結びの改ざん）」と「アニメーション案のB（読み込み時明滅）・D（間欠的な乱れ）」の組み合わせを選択したため、以下を実装した。
  - **メリハリ化**: 本文の揺らぎ文字（1文字ずつ`<span>`でtransform/color/opacityをランダム化している箇所、計710文字）のうち、通常の文字は揺れ幅（translate/rotate）を約45%に減衰し不透明度の下限を0.85へ引き上げて静める一方、由来記の核心語（天女・磔・儀・祟り・供え・凶事・縛る、計24文字）だけ色を`#8a0000`に統一し不透明度1・太字・揺れをやや強調して、既存の「均一に揺れて何が異常か埋没する」状態から強弱のあるコントラストへ変更した。文字自体（テキスト内容）は一切変更していない。
  - **サイドバーのエラー情報追加**: 既存の`Fatal error`ボックスに「最終更新: 1978-00-00 00:00」（存在しない日付）「上書き試行: 3回（失敗）」の2行を追加した。
  - **結びの改ざん演出**: 最後の一文「以上、聞き伝えのままここに記す。」の後に、同じ書き出しを繰り返した上で「以上、聞き伝えの……」と尻切れにする文を追加し、末尾に向けて不透明度を下げるフェードアウトを付けた。
  - **アニメーション**: `@keyframes kiroku-flicker-in`（読み込み時に不透明度が数回明滅した後1で静止する、`animation-iteration-count:1`相当のsteps関数）をサイトタイトル・お知らせ見出し・エラー情報見出しに適用した。`@keyframes kiroku-glitch-pulse`（9秒周期のうち終盤の数%だけ不透明度とtext-shadowが一瞬乱れる）を上記24文字の核心語spanに適用し、文字ごとに`animation-delay`をずらして同期しないようにした。いずれも各spanの`transform`（文字ごとに固有のtranslate/rotate値）には触れず、opacity/text-shadowのみをアニメーションさせることで、既存の「揺らぎ文字」の見た目を壊さないようにした。
- 実装はPythonスクリプトでHTMLをパースし、正規表現でspanのstyle属性を解析・再生成する形で行った（710文字の手作業編集は非現実的なため）。

### 結果
- `<span>`・`<p>`・`<div>`のタグ開閉数が編集前後で一致すること（span: 710→720、追加分は結びの改ざん演出で足した文字数と一致）を確認した。
- Playwright（`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`）でレンダリングし、フルページのスクリーンショットで由来記の核心語のみが赤字・太字で強調されコントラストが出ていること、サイドバーの追加行が表示されていること、結びの改ざん文が意図通り表示されフェードアウトしていることを目視確認した。
- 読み込み直後（100ms時点）と1.6秒後の2時点でスクリーンショットを比較し、`kiroku-flicker-in`が読み込み時にサイトタイトル等を明滅させた後、ループせず不透明度1で静止することを確認した。
- スクリーンショット上で「祟」の字が「崇」に見える箇所があったため、ソース内の実際の文字（Unicodeコードポイント）を確認した。本文テキストは今回一切書き換えておらずソース上は「祟」のままであり、見え方の違いはフォント描画によるものと判断した。

### 次回開始位置
- 特になし。Bで指定した「常時動き続けない」という制約と、Dで指定した「間欠的」という制約は、9秒周期のうち数%のみ乱れる設計で満たしている。今後さらに演出を追加する場合は、既存のGRAYSパレット・KEYWORDSリスト（本エントリのスクリプトに埋め込み、ファイルとしては保存していない）を踏まえて拡張する。

---

## 2026-09-02 T-004: T-003のメリハリ化をユーザー指摘により撤回

### 実施内容
- ユーザーから、T-003のメリハリ化（核心語24文字を赤字・太字・不透明度1に統一する静的な強調）が「チープな演出になっており、相対的に恐怖効果が薄まっている」という指摘を受けた。強制的な色分け・太字は「重要語をハイライトツールで塗った」ような見た目になり、恐怖の対象を明示的に説明してしまう点で、由来記全体が持つ「見る人が自力で異常に気づく」という不気味さと相性が悪いと判断し、静的な強調を全廃する方針で対応した。
- git履歴からT-003適用前の`kiroku.html`（コミット2d807ec時点）を取得し、そこから作り直した。核心語24文字の`transform`・`color`・`opacity`・`font-weight`は原文のランダム値のまま一切変更せず、`kiroku-glitch-pulse`アニメーション（9秒周期のうち終盤数%だけ一瞬乱れる、文字ごとにdelayをずらす）のみを追加した。静止画としては原文と見分けがつかず、時間が経ってはじめて一部の文字がまれに揺らぐという形にした。
- サイドバーのエラー情報追加・結びの改ざん演出・読み込み時の一度きりの明滅（`kiroku-flicker-in`）は、ユーザーからの指摘対象ではなかったためそのまま維持した。
- 作り直しの過程で、対象行の検出条件を`span数>20`としていたところ、結びの一文（16 span）がこの条件から漏れて対象外になり、代わりに直前の段落（「今も、湖に、山に、物あるという。里の暮らしの中にもあるという。」、31 span）が誤って結びの改ざん文で上書きされる実装ミスを自己発見した。対象行の検出を「spanを含むか」という条件に修正し、両方の段落が正しく残ることを確認した。

### 結果
- `<span>`・`<p>`・`<div>`のタグ開閉数が編集前後で一致することを確認した（span: 710→720、内訳はT-003と同じ）。
- Playwrightでレンダリングし、核心語が静止時には周囲の文字と見分けがつかないこと、「今も、湖に…」の段落と「以上、聞き伝えの…」の結びの改ざん文の両方が欠落なく表示されることを確認した。

### 次回開始位置
- 特になし。
